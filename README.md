janus-gateway build
===================

Be sure to clong recursively or
```
$ git submodule init
$ git submodule update
```

Build
-----

APPDIR is the install location for janus gateway

```
$ make APPDIR=/app
```
